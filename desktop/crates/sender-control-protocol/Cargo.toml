[package]
name = "sender-control-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
jsonschema = "0.29"
serde_json = "1.0"

[lints]
workspace = true
