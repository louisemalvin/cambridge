package dev.mobilewebcam.sender.config

object AppDefaults {
    const val defaultProfileId: String = "1080p30"
}

object CameraZoom {
    /** 1x is the optical/default view used when a camera session opens. */
    const val DEFAULT_ZOOM_RATIO: Float = 1.0f
}
