package dev.mobilewebcam.sender.media.camera

/** Camera anti-banding choices exposed by Camera2 when the selected lens supports them. */
enum class AntiFlickerMode {
    AUTO,
    HZ_50,
    HZ_60,
}
