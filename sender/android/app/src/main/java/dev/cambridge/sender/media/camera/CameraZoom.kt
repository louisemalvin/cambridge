package dev.cambridge.sender.media.camera

/** Camera zoom values shared by camera state and its presentation models. */
object CameraZoom {
    /** 1x is the optical/default view used when a camera session opens. */
    const val DEFAULT_ZOOM_RATIO: Float = 1.0f
}
